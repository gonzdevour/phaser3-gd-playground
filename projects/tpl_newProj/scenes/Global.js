import 'phaser';
import * as SceneKey from '../settings/SceneKey.js';

class Global extends Phaser.Scene {
    constructor() {
        super({
            key: SceneKey.Global
        })
    }
    preload() {
    }
    create() {
    }
    update() {
    }
}
export default Global;