//定義scene key
//scene key會拿來start scene用，以字串形式不方便自動完成，變數化較好用

const Boot = 'boot';
const Global = 'global'
const Home = 'home';
const Menu = 'menu';
const Game = 'game';
const Result = 'result';

export {
    Boot,
    Global,
    Home,
    Menu,
    Game,
    Result,
}